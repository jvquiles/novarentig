# Environment variables for local docker-compose
# WARNING: Do not commit secrets to source control. Use secrets manager or CI secrets in production.

# MongoDB credentials (used by mongo service)
MONGO_INITDB_ROOT_USERNAME=mongo
MONGO_INITDB_ROOT_PASSWORD=mongo
MONGO_DB_NAME=novarenting